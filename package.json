{
  "name": "navigation-telecom",
  "dependencies": {
    "parsel": "^0.3.0",
    "three": "^0.162.0",
    "three-pathfinding": "^1.1.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "vite": "^5.1.6"
  },
  "scripts": {
    "deploy": "npx vite build && gh-pages -d dist -b gh-pages"
  }
}
